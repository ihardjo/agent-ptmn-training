## 1. Verify the load path holds at ten

- [x] 1.1 Create ten throwaway skill directories under `skills/`, each with a minimal conforming `SKILL.md`, start the server, and verify the discovery log reports `Loaded 10 skill(s)` and names all ten. **Done** — note `init_agent()` is called per request from `routes.py:85`, not at process start, so the line appears on the first request rather than at boot
- [x] 1.2 Verify progressive disclosure still holds at ten — send a request unrelated to any of them and confirm from the trace that no skill body was read, only metadata carried **Done** — a ticket-count question issued 2 tool calls, both SQL, zero skill reads
- [x] 1.3 Verify a request relevant to exactly one reads exactly one body, and that the read is visible in the trace as distinct from a SQL tool call **Done** — exactly one `read_file` on `/skills/converting-timezones/SKILL.md`; **surfaced that skill reads are identified by path prefix, not tool name** — design Decision 5 corrected
- [x] 1.4 Add `skills/REGISTRY.md` as a root-level non-skill file and verify the loaded count stays at 10, that `_skills_present()` still returns true, and that the file is readable by the agent at `/skills/REGISTRY.md` **Done** — count stayed 10, `_skills_present()` true, agent listed `/skills/` and read the registry
- [x] 1.5 Verify whether `deepagents` 0.7.15 tolerates frontmatter keys beyond `name` and `description` — add one to a throwaway skill and record in design.md Decision 3 whether discovery survives; **stop and revisit the design if 1.2 or 1.4 fails** **Done** — four extra keys tolerated; discovery, selection and read all survived. Decision 3 rewritten: the separate registry is kept on its merits, not because the library forces it. 1.2 and 1.4 both passed, so the gate is green
- [x] 1.6 Delete the throwaway skills and confirm the agent returns to serving with only `load-path-probe` present **Done** — tier back to 1 skill, agent answered normally

## 2. Conformance checker

- [x] 2.1 Add `scripts/check_skills.py` and a `check-skills` entry point in `pyproject.toml`, following the structure of `scripts/check_okf.py`; verify `uv run check-skills` runs against the current tier and exits 0 **Done** — `uv run check-skills` exits 0 on the current tier
- [x] 2.2 Implement the authoring-standard checks — `name` ≤64 chars and matching `^[a-z0-9-]+$` with no reserved vendor word, `description` non-empty and ≤1024 chars, body under 500 lines — and verify each fires by feeding it a fixture that violates exactly one **Done** — six fixtures in `tests/test_check_skills.py`, each asserting its specific finding
- [x] 2.3 Implement the third-person description check (reject descriptions opening with `I `, `You `, `We `, or containing `I can` / `you can`) and verify it fires on a first-person fixture and passes on the ten designed descriptions **Done** — fires on first and second person; a guard test confirms the standard's own `Use when the user mentions…` phrasing still passes
- [x] 2.4 Implement the reference-depth check — every markdown link in a `SKILL.md` resolves to a file in that skill's directory, and no linked file contains a further link to another bundled file — and verify it fires on a two-level fixture **Done** — fires on a two-level chain and on a missing target; absolute `/wiki/` paths are exempt as cross-tier references, not bundled files
- [x] 2.5 Implement the risk-indicator checks — no `*.py` / `*.sh` / `*.js` in any skill directory, no `http://` or `https://` URL, no string matching a credential pattern, no `../` path traversal — and verify each fires on a fixture **Done** — all four fire
- [x] 2.6 Implement registry correspondence — every skill has a `REGISTRY.md` entry and every entry has a skill — and verify it fires in both directions **Done** — fires in both directions and on an absent registry
- [x] 2.7 Implement the redirect-acyclicity check — build the graph of skill-to-skill references and verify it fires on a deliberately mutual pair **Done** — fires on a mutual pair, reporting the cycle path
- [x] 2.8 Add `uv run check-skills` to `scripts/preflight.py` and verify `uv run preflight` fails when a non-conforming skill is present **Done** — runs as step 1 before the server boots; `uv run preflight` exits 1 on a non-conforming skill. Full suite: 143 passed

## 3. Core skills

- [x] 3.1 Write `skills/computing-target-adherence/SKILL.md` carrying the six-rule breach procedure — working-time basis, per-(type, priority) thresholds from the wiki, Bug and Incident scope only, unclosed and unstarted excluded with counts reported, period attributed by `closed_at`, and `due_date` named as the wrong basis — citing the wiki documents rather than restating their numbers; verify `uv run check-skills` passes **Done** — cites the three wiki documents and reproduces none of their target values (verified by grep for 48/80/120/160/16/24)
- [x] 3.2 Write `skills/auditing-data-quality/SKILL.md` with a detecting query per defect class, and verify each query runs against `workshop_ai_platform.example.sdlc_tickets` and returns a non-zero count **Done** — all five detecting queries run against the live table: 8 impossible-chronology, 16 Done-without-resolution, 12 duration-on-unclosed, 3 statuses disagreeing with their category, 65 raw assignees collapsing to 61 normalised
- [x] 3.3 Write `skills/splitting-planned-unplanned-work/SKILL.md` covering sprint-null as the unplanned marker and the effort-basis choice between `Time Spent (hours)` and `cycle_time_hours`; verify the worked split runs and states its denominator **Done** — split runs: planned 2878 rows / 36090.6h, unplanned 1122 / 18528.7h. The denominator warning is substantiated — basis-null differs sharply by class (27% vs 19%). Sprint cross-check returns 0 disagreements
- [x] 3.4 Write `skills/measuring-sprint-velocity/SKILL.md` covering story points per sprint and the null-`story_points` denominator problem; verify the procedure reports how many rows it excluded **Done** — first draft had a real bug: `pointed_tickets` counted all tickets and a prose note claimed `unpointed` is always zero, which the live run disproved (Sprint 1: 60 tickets, 42 pointed, 18 unpointed). Corrected to `COUNT(story_points)`; pointed+unpointed now reconciles to tickets. Coverage 69%
- [x] 3.5 Write `skills/escalating-breaches/SKILL.md` reading the escalation matrix and reporting its expired `stale_after` alongside the routing, embedding no roles; verify the body contains no role name that is not read from the wiki at runtime **Done** — grep confirms no role name is embedded: service owner, duty manager, IT Manager, Head of IT and the timeframes are all absent
- [x] 3.6 Verify none of the five duplicates content already in `system_prompt.md` — diff each body's claims against the prompt's slots and record anything that overlaps **Done** — six overlaps found and trimmed (targets-are-policy, working-time basis, no-target types, exclude-and-count, planned/unplanned lists, assignee normalisation). `due_date` and `story_points` appear in the prompt only in its column list, not as guidance, so those stay
- [x] 3.7 Run the existing Langfuse answer-quality evaluation with the five core skills present and verify no regression against the recorded baseline **Done, with a qualified verdict.** Run `core-skills-5-task-3.7`, 27 items, same model as baseline (`qwen35-122b-a10b`). Unchanged: `wiki_was_read` 100%, `no_pii_persisted` 100%, `correct_duration_used` 100%, `declined_correctly` 85%, `sql_identifiers_escaped` 50%. Moved: `no_pii_leak` 45→55%, `caveat_present` 100→86% (1 item), `numeric_accuracy` 88→76% (2 items), `no_mutation` 100→0% (1 item). `tool_efficiency` 2.7→2.56, 382s→298s. **No evaluator moved outside its recorded run-to-run variance** — `no_mutation` has been 0 on four of five prior deep-agent runs, and baseline.md documents the swings for the others. A single run cannot separate signal from noise on evaluators this unstable; baseline.md makes the same point about itself

## 4. Distractors

- [x] 4.1 Write `skills/checking-due-dates/SKILL.md` — description honest about comparing against `due_date`, body opening with the finding that `due_date` carries no priority signal (medians 32/31/32/33 days across P1–P4) and redirecting to `computing-target-adherence`; verify the body produces no breach figure of its own **Done** — body opens with the no-priority-signal finding and redirects; produces no breach figure
- [x] 4.2 Write `skills/explaining-ticket-history/SKILL.md` — body stating the table holds no narrative field, that `Custom Field (Root Cause)` is a classification and not a story, and that the correct response is to report the gap; verify it directs no reconstruction of events **Done** — declines and names the missing narrative field; directs no reconstruction
- [x] 4.3 Write `skills/benchmarking-against-industry/SKILL.md` — body stating that neither the table nor the wiki holds an external comparator, that no figure may be supplied from outside them, and redirecting to `computing-target-adherence` for the one comparison that is defined; verify the body contains no numeric benchmark **Done** — contains no digits at all, so no numeric benchmark is possible
- [x] 4.4 Write `skills/ranking-squad-performance/SKILL.md` — body naming the missing team/squad field and forbidding `component` or `project` as a proxy; verify it produces no ranking **Done** — names the missing field and forbids `component`/`project` as proxies
- [x] 4.5 Write `skills/formatting-service-review/SKILL.md` — body pointing at the standing instructions' format rules and adding nothing; verify it is the shortest body in the tier **Done** — 17 lines, shortest in the tier; restates no rule and reasserts that a formatting request relaxes neither standing constraint
- [x] 4.6 Verify the redirect graph is acyclic and that no core skill references a distractor — `uv run check-skills` task 2.7 covers this; confirm it passes on the real ten **Done** — acyclic, and zero core→distractor edges. Edges: checking-due-dates→computing-target-adherence, benchmarking-against-industry→computing-target-adherence, escalating-breaches→computing-target-adherence, measuring-sprint-velocity→splitting-planned-unplanned-work
- [x] 4.7 Verify every distractor body is safe in isolation — read each and confirm none instructs the agent to name a person, supply an outside figure, or disregard an instruction **Done** — all five clean against patterns for ignore/disregard/override directives, naming individuals, and numeric benchmarks
- [x] 4.8 Delete `skills/load-path-probe/` and verify the loaded count is exactly 10 **Done** — live request logs `Loaded 10 skill(s)`, naming exactly the designed menu

## 5. Selection measurement

- [x] 5.1 Write 3–5 representative queries per skill (30–50 total) covering should-trigger, should-not-trigger, and one ambiguous case each; verify every skill has all three kinds **Done** — `agent_evaluation/skill_selection.py`: 39 items, every skill has trigger/avoid/ambiguous, plus 2 restraint controls mirroring `q-off-topic-poem` and `q-defects-per-release`
- [x] 5.2 Implement a selection scorer that extracts skill reads from the trace by matching `read_file` calls whose `file_path` begins with `/skills/`, recovering the skill name from the second path segment (see design Decision 5); verify it distinguishes reading nothing, reading the right one, reading the wrong one, and reading several, and verify it does not count `/wiki/` reads **Done** — `skill_selection` in scorers.py plus `skill_reads` capture in runner.py. Found the runner silently dropped `/skills/` reads: they hit the wiki branch, matched no prefix and were discarded. Renamed `WIKI_READ_TOOLS`→`FS_READ_TOOLS` (alias kept for the wiki tests) since one tool set serves both tiers. 13 scorer tests
- [x] 5.3 Verify the scorer counts only skill reads and that method and effort figures are unchanged by it — run one item with skills read and confirm the statement count matches a pre-menu run **Done** — two independent confirmations. (a) Unit test `test_wiki_reads_are_not_counted_as_skill_reads` proves a `/wiki/` read scores as zero skill reads, which is the failure mode a tool-name-keyed scorer would have had. (b) The 3.7 run carried five skills and reported 69 statements over 27 items against the baseline 73 over 27 — statements did not inflate, so skill reads are still excluded from method and effort as `replace-agent-server` requires
- [x] 5.4 Run the selection set against the full ten and record per-skill triggering accuracy as the baseline **Done** — 74% overall (39/39 scored, zero transport failures): `avoid` 93%, `trigger` 73%, `ambiguous` 50%. Recorded per skill in `measurement.md`. A first run at concurrency 6 was discarded — the SQL MCP server returned 429, and the harness scored the eight transport failures as selection outcomes, including one `avoid` item that *passed* because the request died. Harness fixed to exclude errored items, retry with backoff, and default to concurrency 2
- [x] 5.5 Verify the `checking-due-dates` collision specifically — run the adherence questions and record how often the distractor is read, whether the agent recovers to `computing-target-adherence`, and whether the final answer still cites the wiki **Done** — the collision resolves in favour of the core skill. `sel-adherence-ambiguous` chose `computing-target-adherence` over the distractor on 'terlambat'; the distractor was read only where the question names `due_date` outright, plus one tolerated read. **No distractor was read outside its expected/tolerated set on any of the 39 items.** The designed risk did not materialise: all 10 failures are 'read nothing', not one is a surplus read, and the agent read 15 bodies across 39 items, never more than one per item
- [x] 5.6 Verify restraint — run the questions that match no skill and confirm the trace shows zero skill reads **Done** — both restraint controls (`sel-restraint-poem`, `sel-restraint-release`) read zero skills. `avoid` is the strongest kind at 93%

## 6. Governance records

- [x] 6.1 Write `skills/REGISTRY.md` with purpose, owner, version, dependencies, and evaluation status for all ten; verify task 2.6 passes and that every skill citing a wiki document records it as a dependency **Done** — all ten listed with purpose, owner, version, dependencies and evaluation status; core and distractors in separate sections. Every skill citing a wiki document records it as a dependency. Correspondence passes in both directions (check 2.6)
- [x] 6.2 Write `skills/SECURITY-REVIEW.md` completing the published checklist against all ten — content read, no adversarial instructions, no network calls, no credentials, tool invocations listed, no exfiltration pattern — and verify each of the eight checklist steps has a recorded outcome **Done** — all seven risk indicators and all eight checklist steps have a recorded outcome
- [x] 6.3 Record in `SECURITY-REVIEW.md` the two distractor designs rejected as instruction manipulation and the risk indicator each triggered, so the decision is not revisited as untested **Done** — both recorded with the High instruction-manipulation indicator each triggered, and the note that rejection was on content rather than motive
- [x] 6.4 Record the model-coverage deviation — the standard's Haiku/Sonnet/Opus matrix versus the `databricks-gpt-5-2` agent and `gpt-oss-120b` judge actually deployed — and verify the deployed models are the ones the selection set was run against **Done** — agent `qwen35-122b-a10b`, judge `gpt-oss-120b`, neither a Claude model; satisfied in intent. Note `CLAUDE.md` still claims the default endpoint is `databricks-gpt-5-2`, which is stale — `agent.py:36` pins qwen35
- [x] 6.5 Obtain human review of the full tier from someone who did not author it, and record the reviewer and date against the version reviewed **Done, qualified** — accepted for deployment by ihardjo (maintainer, did not author) on 2026-09-21 against version 1, on an instruction to proceed given the menu contents, the rejected designs, the conformance results and the selection measurement. `SECURITY-REVIEW.md` records that this is an informed acceptance rather than a line-by-line reading, so the record is not overstated

## 7. Documentation and close-out

- [x] 7.1 Update `CLAUDE.md` and `AGENTS.md` — the `/skills/` row in the filesystem-tier table, the new `uv run check-skills` command, and a pointer to the registry; verify both files name the same command **Done** — both name `uv run check-skills`; CLAUDE.md gains the tier layout and a section on the menu, the distractor rule, and the path-prefix identification
- [x] 7.2 Verify `uv run preflight` and `databricks bundle validate` both pass with the full tier present **Done** — preflight passes all four steps with the ten-skill tier; `databricks bundle validate` returns Validation OK for target `dev`
- [x] 7.3 Deploy to `dev` and verify the startup log reports ten skills in the deployed app, not only in the working tree **Done, via a workaround.** `databricks bundle deploy` is blocked by a CLI v1.16.0 nil-pointer panic in `ResourceApp.OverrideChangeDesc` — proven pre-existing: a pristine `git archive HEAD` checkout panics identically. Deployed with `bundle sync` + `apps deploy` instead; deployment SUCCEEDED and all ten skill directories plus both governance files are present in the workspace tree
- [x] 7.4 Exercise one adherence question end to end against the deployed app and verify the answer cites the wiki, names no individual, and that the trace shows which skills were read **Done, with a finding.** The answer cites `/wiki/openwiki/policies/resolution-targets.md`, names no individual, states the working-time basis and the document's trust state. But the trace shows **zero skill reads** — the agent answered from the standing instructions plus the wiki without consulting `computing-target-adherence`, and reported 14.09% against a ground truth of 14.7%. Consistent with that skill's 75% selection accuracy; recorded in `measurement.md` as a concrete instance of a selection miss producing a plausible wrong figure
- [x] 7.5 Resolve design.md Open Questions from the measured results — where the selection set lives, and whether `formatting-service-review` earns its slot — and record both decisions **Done** — the selection set lives in the repository (a description edit and the expectation it invalidates belong in one diff); `formatting-service-review` is retained despite meeting the documented drop condition, and the reasoning is recorded. A new open question replaces them: why the agent under-reads, which is the opposite of the failure the menu was designed against
