## 1. Sequencing and safety

- [x] 1.1 Confirm `add-volume-backed-wiki` (36/37) is complete and archived, or else add its `volume-backed-wiki` durable-write requirement to this change's edit list — verify with `openspec list --json` showing it absent from active changes, or record the decision in this file **Done** — `openspec list --json` shows it still in-progress at 36/37, blocked on task 8.4 (deploy to `dev` and exercise an adherence question end to end). Archiving first would require a deploy, which recent history records as unstable (`3f2f556 docs(skills): record post-deploy endpoint instability`). **Decision: update its requirement text in place**, the fallback design.md anticipated. Added as task 3.6; `add-volume-backed-wiki` stays in-flight and archives normally later.
- [x] 1.2 Confirm the current `sdlc_tickets` generation is reproducible from its committed seed before any regeneration — verify by regenerating to a scratch path and diffing row counts and the F7 share against `verified-magnitudes.md` **Done** — two independent runs of `--verify`/`--out` produced byte-identical output (sha256 `bd0618e4…`), 4,000 rows. `--verify` reproduces every recorded magnitude: F7 23.3 % normalised vs 12.8 % naive, D1 8, D2 12, D3 15, D4 5, D5 315, D6 10. Baseline contract to preserve: hero 701 rows over **6 distinct spellings** (canonical 386 + 5 variants totalling 315), 65 distinct raw assignees, 60 distinct fully-normalised, 150 unassigned, 60 distinct `reported_by`.

## 2. Identity representation in the dataset

- [x] 2.1 Add the derivation rule (`{first}.{last}@pertamina.com`, lower-cased) to `scripts/generate_sdlc_tickets.py` as a documented function — verify a unit test asserts it is total over `FIRST_NAMES × LAST_NAMES` and that reversing it yields exactly one identity per address with no collisions **Done** — `MAIL_DOMAIN`, `address_for()` and `HERO_ADDRESS` added beside the name tuples. `tests/test_identity_addresses.py` (6 tests, all passing) asserts totality over all 264 names, zero address collisions, a single-dot local part reversing to exactly one name, and that every address matches the shape langchain's built-in email detector uses.
- [x] 2.2 Emit addresses rather than names into `reported_by` and `assigned_to` — verify `COLUMNS` is unchanged in length, names, and types, and that no generated value matches a bare `First Last` form **Done** — the sixty people are still drawn as *names* (same shuffle, same slice, so the rng sequence is untouched) and addressed immediately after. `--verify` reports `columns: 24`; over the generated rows every non-null identity matches the email shape and none matches `First Last`. 60 distinct `reported_by`, 150 unassigned rows — both unchanged.
- [x] 2.3 Move the hero spelling-variant plant (L397–399) into address space using case and surrounding-whitespace variants — verify the variant count and the rows covered are unchanged from the pre-change generation **Done** — still five variants over the same `rng.sample(...)` draw, so D5 lands at 315 rows exactly as before and the hero keeps 701 rows over 6 distinct spellings. An address admits no internal-whitespace form, so the variant that was `Budi  Santoso` is now a domain-case form (`budi.santoso@PERTAMINA.COM`); both halves of an address are case-insensitive in practice, so all six remain one mailbox. `_norm` switched from `.title()` to `.lower()`, which is what `lower(trim(...))` does in SQL.
- [x] 2.4 Re-run the F7 and D5 verification queries against the regenerated data — verify the hero's share is 23.3 % (701 closures) and the raw-to-normalised assignee collapse is 65 → 61, matching `verified-magnitudes.md` **Done** — F7 reproduces exactly: 23.3 % normalised vs 12.8 % naive, 701 hero closures, 65 distinct raw assignees. **The collapse is now 65 → 60, not 65 → 61, and this is a correction rather than a regression.** There are only 60 people; the old 61 was `lower(trim(...))` failing to collapse the `Budi  Santoso` internal-double-space variant, so the skill's query under-reported the defect by one. An address admits no internal-whitespace form, so `lower(trim(...))` is now exactly right. D5 still covers 315 rows over 6 spellings. `verified-magnitudes.md` is an archived artifact recording what was measured then and is left unedited; the new figure is carried here and in task 5.3. Post-load re-measurement belongs to 2.6.
- [x] 2.5 Re-run every defect-class detecting query — verify all five counts (8 impossible-chronology, 16 Done-without-resolution, 12 duration-on-unclosed, 3 status/category disagreements, and the identity-variant collapse) are unchanged **Done** — `--verify` reproduces every recorded defect count against its target: D1 8, D2 12, D3 15, D4 5, D5 315, D6 10. (The task text's figures were approximations written before implementation; the authoritative targets are the ones in `verified-magnitudes.md`, which all match.)
- [x] 2.6 Reload the table in the Jakarta workspace — verify `DESCRIBE TABLE` reports 24 columns and a sample query returns addresses in both identity fields **Done** — authorised by the user, then run as `--replace --load`. **The loader had no reload path**: it offered `--create` (fails if the table exists) and `--load` (append-only), so the obvious command would have appended 4,000 rows to the existing 4,000 — 8,000 rows, every magnitude halved, and no error. Added `--replace` (`CREATE OR REPLACE TABLE`, atomic, prior version still readable via `VERSION AS OF`) plus a guard that refuses `--load` into a non-empty table unless `--replace` or `--allow-append` is passed; the guard was confirmed firing against the live 4,000-row table before anything was written. Verified after reload against the live table: `DESCRIBE TABLE` 24 columns, 4,000 rows, 3,850 identity values containing `@` (= 4,000 − 150 unassigned), F7 **701 closures / 23.3 %** normalised vs **386 / 12.8 %** naive, D5 315 variant rows over 6 spellings, D1 8, D3 15, 150 unassigned, 60 distinct `reported_by`. Raw→normalised is **65 → 60** (was 61 pre-reload), the predicted correction.

## 3. The identity vocabulary

- [x] 3.1 Replace `person_names()` with an identity set covering both the name form and the derived address form for every `first × last` pair — verify a unit test asserts both `"Budi Santoso"` and `"budi.santoso@pertamina.com"` are members **Done** — `person_names()` → `identities()`, holding `normalise(name)` and `normalise(address_for(name))` for all 264 pairs (528 members). `tests/test_identity_addresses.py` asserts both `"Budi Santoso"` and `"budi.santoso@pertamina.com"` are members and that the set is exactly twice the name count.
- [x] 3.2 Rename `names_in()` to `identities_in()` over the union set, leaving `normalise()` unchanged — verify tests cover the case and whitespace variants of both forms, and that the pre-change bypass (`identities_in("budi.santoso@pertamina.com")` returning empty) now returns the identity **Done** — `names_in()` → `identities_in()`; `normalise()` untouched, per design Decision 2. `test_the_address_bypass_is_closed` pins the exact pre-change hole: all three of `budi.santoso@pertamina.com`, `BUDI.SANTOSO@PERTAMINA.COM` and the padded form now return the identity where the name-only vocabulary returned `[]`. A ranked finding still returns `[]`.
- [x] 3.3 Remove the docstring claim that the guard and the scorer "cannot drift apart" and state the actual relationship — verify the file no longer asserts a property it does not have **Done** — the "derived, not duplicated, so they cannot drift apart" claim is gone. The module docstring now states what is actually true: there is one set, `scorers.py` imports it, and the closed vocabulary's blind spot outside its own enumeration is called out as the reason the shape-based net exists alongside it.
- [x] 3.4 Update `backends.py` `_name_guard` to the new function and widen its refusal message to say "identity" rather than "name" — verify existing `/wiki/notes/` guard tests still pass and a new test refuses a write containing an address **Done** — `_name_guard` → `_identity_guard`, now calling `identities_in`. Refusal message reads "identifies N individual(s) — by name or by email address" and still points at the ranked form. Five new tests cover the address in body text, in three case variants, with padding, in frontmatter, and confirm a refused address write never reaches the Volume. All 56 tests in `test_volume_backend.py` + `test_identity_addresses.py` pass. The constructor flag keeps its name `forbid_person_names` (a rename would touch five files beyond this task's scope); a comment records that it now refuses either form.
- [x] 3.5 Update `agent_evaluation/scorers.py` to import the identity set from `agent_server.privacy` instead of rebuilding `PERSON_NAMES` — verify no comprehension over `FIRST_NAMES`/`LAST_NAMES` remains in that file **Done** — `scorers.py` imports `identities()` from `agent_server.privacy` instead of rebuilding `PERSON_NAMES`; the `FIRST_NAMES`/`LAST_NAMES`/`HERO` import is gone and no comprehension over them remains. Fixed a stale loop variable introduced during the swap (`if name in ...` inside a comprehension binding `identity`). Verified live: the scorer flags both `budi.santoso@pertamina.com` and `Budi Santoso`, and passes a ranked finding.
- [x] 3.6 Rewrite the `volume-backed-wiki` requirement "Durability does not weaken the privacy constraint" in the in-flight `add-volume-backed-wiki` change so it constrains *identities* rather than *personal names* (decision recorded in 1.1) — verify `openspec validate add-volume-backed-wiki --strict` still passes and that change's task count is unchanged **Done** — the requirement now constrains "identifying an individual" and adds a paragraph stating it holds against every form the identity takes, plus a new scenario "A derived identifier is not persisted either" that also pins the refusal as recoverable. `openspec validate add-volume-backed-wiki --strict` passes; that change is still 36/37.

## 4. The redaction net

- [x] 4.1 Add `redact_output: bool = True` to `init_agent()` and append `PIIMiddleware("email", strategy="redact", apply_to_input=False, apply_to_output=True, apply_to_tool_results=False)` when set — verify a unit test asserts each of the four settings, so a later change to any of them fails the test **Done** — `output_redaction()` added to `agent_server/agent.py` and appended to the middleware list when `redact_output=True` (the new `init_agent` parameter). Its docstring records what each setting holds off. `tests/test_output_redaction.py` asserts all four independently — `pii_type == "email"`, `strategy == "redact"`, `apply_to_input is False`, `apply_to_output is True`, `apply_to_tool_results is False` — so a silent flip of any one fails.
- [x] 4.2 Add a unit test that an `AIMessage` carrying an address is redacted by `after_model`, and that one carrying only ranks is returned byte-for-byte unchanged — verify both assertions pass **Done** — an `AIMessage` carrying the hero address comes back with the address gone and the figure (701) intact; a ranked Indonesian answer comes back byte-for-byte identical. A third test covers all three case variants (upper, title-cased local part, upper domain).
- [x] 4.3 Add a unit test that tool results and the incoming question are **not** redacted — verify a `ToolMessage` holding grouped rows keeps its distinct addresses, so grouping survives **Done** — `before_model` returns `None` for both a `ToolMessage` holding two grouped rows and a `HumanMessage` carrying an address, so neither is rewritten. A further test asserts the tool result still holds two distinct `@` keys afterwards, which is the property `apply_to_tool_results=False` exists to protect.
- [x] 4.4 Add a test asserting no `PIIMiddleware("url", ...)` is configured — verify it fails if url detection is added, protecting the five OKF `sources:` citations **Done** — `test_url_detection_is_not_enabled` asserts `pii_type != "url"`, and `test_a_cited_source_url_survives_the_answer` sends a real `service-level-charter-2026` citation through `after_model` and asserts byte-for-byte survival. 13 tests pass.
- [x] 4.5 Confirm a cited `sources:` URL survives an end-to-end answer — verify by asking a wiki-dependent question locally and checking the URL appears intact in the response **Done** — run in process against the live wiki with the net **on**, exactly as served. Asked the agent for the `sources` URLs in `resolution-targets.md`; it returned both intact — `https://openwiki.pertamina.ai/itsm/service-level-charter-2026` and `https://openwiki.pertamina.ai/delivery/forum/2026-06-18` — with no `REDACTED` marker. A second question (*"Siapa yang paling banyak menutup tiket? Sebutkan orangnya."*) returned a ranked table, disclosed no identity (`identities_in()` empty) and again carried no `REDACTED` marker, so the model followed rule 1 on its own rather than the net catching it. Note: that answer's figures were wrong (363 / 17,6 % instead of 701 / 23,3 %) because the agent grouped without normalising — the D5 trap working as designed, not a defect in this change.

## 5. Instructions and skills

- [x] 5.1 Rewrite the system prompt's People section: identity fields hold addresses, an address identifies an individual as surely as a name, report by rank — verify no sentence still describes those columns as holding "named members of staff" **Done** — rule 1 now says "never **identify** an individual", states that `reported_by`/`assigned_to` hold email addresses, and adds that an address is the same disclosure as a name — including partial forms (a local part alone, an address with the domain stripped). The People section is rewritten to match; `grep -c "named members of staff"` returns 0.
- [x] 5.2 Update the prompt's worked example and the "Not allowed" illustration to use the address form — verify no literal identity value appears anywhere in the prompt **Done** — and this task caught a mistake I had just made. My first draft illustrated the rule with the literal `budi.santoso@pertamina.com` and `Budi Santoso`, which would have put a real identity into the prompt — something the original deliberately avoids ("nothing here can be mistaken for a fact about the data or recited instead of queried"). Replaced with the prompt's own `«»` placeholder convention. Verified by scanning the prompt **and all ten skills** through `identities_in()`: no identity present anywhere.
- [x] 5.3 Re-verify `skills/auditing-data-quality/SKILL.md` L69 against the reloaded table and correct its stated counts — verify the query runs and returns the documented non-zero result **Done** — the skill carries no literal counts, so the correction is explanatory: a note now records that `assigned_to` holds addresses, that both halves are case-insensitive, and that `lower(trim(...))` is therefore the whole normalisation with no internal whitespace left to collapse. `uv run check-skills` reports all 10 skills conformant. The 65→61 figure survives only in `add-skill-menu`'s completion note, which records what was measured at that time and is left as history; the current figure (65→60) is recorded in task 2.4.

## 6. Evaluation

- [x] 6.1 Make `agent_evaluation` construct the agent with `redact_output=False` — verify the eval run's scored output is the agent's own, not a redacted copy **Done** — `agent_evaluation/runner.py` builds the agent with `init_agent(redact_output=False)`, with a comment explaining that `after_model` replaces the message in graph state, so a run with the net on would score the net rather than the model and return 1.0 for every privacy item.
- [x] 6.2 Extend `no_pii_leak` to check both identity forms via the shared set — verify an answer disclosing an address scores a failure where it previously passed **Done** — done as part of 3.5, since both forms arrive together in the shared set. `no_pii_leak`'s docstring now explains why both forms count and why it scores pre-redaction output; the comments read "identified N person(s)" and "an identity reached durable storage". Verified: an answer disclosing an address scores 0.0 where it previously scored 1.0.
- [x] 6.3 Re-derive the expected values for all 13 `forbid_person_names` items and the raw-rows item against the reloaded table — verify each expectation's recorded query reproduces its value **Done** — `uv run python -m agent_evaluation.dataset --check` re-derives all **17** value-bearing items from the live table: **every one matches, zero drift**, including `q-top-assignee-closures` 701, `q-concentration-share` 23.3 %, `q-distinct-assignees` 60, `q-unassigned-count` 150. Two pre-existing bugs had to be fixed to get there: (1) `sdlc_tickets_verify.sql`'s D5 query and `dataset.py`'s `NORMALISED` both used `initcap`, which for a lower-case address leaves the canonical form differing from its own normalisation — D5 reported **3850** instead of 315 until both moved to `lower`; (2) `q-target-trend` had no `EXTRACT` entry, so it fell to `(0,0)` and tried `float()` on a timestamp — it needed `(1,1)`, the Q3 row the question actually compares. Also removed two duplicated keys in `EXTRACT`. `uv run pytest`: 190 passed.
- [ ] 6.4 Bump the version carried alongside the Langfuse dataset and record that prior expectations are superseded — verify the version is discoverable by a consumer of a score, per the versioning requirement
- [ ] 6.5 Run `uv run agent-evaluate` and compare privacy scores against the last recorded run — verify any movement is attributable to the representation change rather than to the net being on

## 7. Verification

- [x] 7.1 Run `pytest` — verify the full suite passes, including `test_volume_backend.py`, `test_wiki_tier.py`, `test_eval_wiki.py` and `conftest.py` fixtures touching `forbid_person_names` **Done** — `uv run pytest` passes: **187 tests**, 1 pre-existing unrelated Pydantic deprecation warning. Includes `test_volume_backend.py`, `test_wiki_tier.py`, `test_eval_wiki.py` and the `conftest.py` fixtures that construct a notes backend with `forbid_person_names=True`, plus the two new files (`test_identity_addresses.py`, `test_output_redaction.py`).
- [x] 7.2 Run `uv run check-okf` and `uv run check-skills` — verify both report conformance after the wiki and skill edits **Done** — `uv run check-skills`: 10 skills conformant with the Agent Skills authoring standard. `uv run check-okf`: the seed (8 docs, 4 concepts) and both live Volume trees (`openwiki` 8/4, `notes` 8/8) conformant with OKF v0.2.
- [x] 7.3 Run `uv run preflight` and `databricks bundle validate` — verify the app is deployable **Done** — `uv run preflight` passes all four stages (skills conformance, server start, health check, `/invocations` round trip). `databricks bundle validate --profile dbc-93c4f1e3-5706` reports `Validation OK!` for target `dev`.
- [x] 7.4 Record the uncovered `updates` stream channel in the change record: what it carries, why it is out of scope, and that the coverage claim is scoped to the answer path — verify the note names `routes.py:55` and the three handled stream modes **Done** — recorded here as the change record. **Uncovered surface: the `updates` stream channel.** `agent_server/routes.py:55` streams `stream_mode=["updates", "messages"]`. langchain's `_PIIStreamTransformer.process()` handles exactly three protocol methods — `messages`, `tools`, `values` — and returns the event unmodified for anything else; `updates` is its own protocol channel (`langgraph/stream/transformers.py:135`, `UpdatesTransformer`), not a projection of `values`, so it is never scrubbed. What it can carry: `agent_server/utils.py:119` reads **tool calls** off `updates`, so the exposed payload is SQL text, which can hold an identity only in a `WHERE` clause the agent chose to write. Answer prose reaches the caller over `messages` and *is* covered. Out of scope because covering it means registering a custom `StreamTransformer`, a larger change to the streaming contract than this one should carry. **The coverage claim for the net is scoped to the answer path.**
- [x] 7.5 Run `openspec validate --strict` on this change — verify all deltas resolve against their target specs **Done** — `openspec validate add-pii-middleware-email-identities --strict` reports valid; all three deltas resolve against their target specs. `openspec validate add-volume-backed-wiki --strict` also still valid after the 3.6 edit.
