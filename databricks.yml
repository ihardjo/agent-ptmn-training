bundle:
  name: agent_langgraph

resources:
  apps:
    agent_langgraph:
      name: "agent-ptmn-training"
      description: "LangGraph agent application"
      source_code_path: ./
      config:
        command: ["uv", "run", "start-app"]
        env:
          - name: API_PROXY
            value: "http://localhost:8000/invocations"
          - name: CHAT_APP_PORT
            value: "3000"
          - name: CHAT_PROXY_TIMEOUT_SECONDS
            value: "300"
          # Langfuse observability (optional — agent starts without these)
          - name: LANGFUSE_PUBLIC_KEY
            value: "pk-lf-091592b0-696e-4037-bd8b-8ebba8ed862e"
          - name: LANGFUSE_SECRET_KEY
            value_from: "langfuse-secret"
          - name: LANGFUSE_HOST
            value: "https://agentmonitoring.pertamina.ai"
          # Jakarta workspace — managed SQL MCP over workshop_ai_platform.example
          - name: DATABRICKS_JAKARTA_HOST
            value: "https://dbc-86b2f2e8-b955.cloud.databricks.com"
          - name: DATABRICKS_JAKARTA_CLIENT_ID
            value_from: "databricks-jakarta-id"
          - name: DATABRICKS_JAKARTA_CLIENT_SECRET
            value_from: "databricks-jakarta-secret"
          # Volume-backed wiki — OKF bundle on a Unity Catalog Volume in Jakarta
          - name: DATABRICKS_WIKI_VOLUME
            value: "/Volumes/workshop_ai_platform/example/agent_wiki"

      # Resources which this app has access to
      resources:
        - name: 'langfuse-secret'
          secret:
            scope: agent-ptmn-training
            key: langfuse-secret-key
            permission: 'READ'
        - name: 'databricks-jakarta-id'
          secret:
            scope: agent-ptmn-training
            key: databricks-jakarta-id
            permission: 'READ'
        - name: 'databricks-jakarta-secret'
          secret:
            scope: agent-ptmn-training
            key: databricks-jakarta-secret
            permission: 'READ'

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-93c4f1e3-5706.cloud.databricks.com

  prod:
    mode: production
    # workspace:
    #   host: https://...
    resources:
      apps:
        agent_langgraph:
          name: agent-langgraph
